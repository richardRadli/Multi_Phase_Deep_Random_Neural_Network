{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "FCNN Configuration",
  "type": "object",
  "properties": {
    "dataset_name": {
      "type": "string",
      "enum": ["connect4", "isolete", "letter", "mnist", "mnist_fashion", "musk2", "optdigits", "page_blocks", "satimages", "shuttle", "spambase", "usps"],
      "description": "The name of the dataset to be used."
    },
    "patience": {
      "type": "integer",
      "description": "The number of epochs after the training should be stopped if the validation loss is not decreasing."
    },
    "valid_size": {
      "type": "number",
      "description": "Percentage of the size of the validation set."
    },
    "number_of_tests": {
      "type": "integer",
      "description": "Number of tests to be performed."
    },
    "seed": {
      "type": "boolean",
      "description": "Controls the way to generate random values. If set to True, the same values will be generated in every execution."
    },
    "learning_rate": {
      "type": "number",
      "description": "Value of the learning rate."
    },
    "batch_size": {
      "type": "integer",
      "description": "The size of the batches."
    },
    "epochs": {
      "type": "integer",
      "description": "Number epochs."
    }
  },
  "required": ["dataset_name", "patience", "valid_size" ,"number_of_tests", "seed", "learning_rate", "batch_size", "epochs"]
}
